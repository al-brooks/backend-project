<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/output.css" />
    <title>Login</title>
</head>
<body id="userLoginBody">
    <header id="userAuthHeader">
        <nav id=userAuthNav>
            <div id="userAuthSearch">
                <a href="../" id="logoImg"></a>
                <form method="POST" action="/search/movies">
                    <input id="search" type="text" name="search" placeholder="search movies"/>
                    <button id="searchFormBtn"></button>
                </form>
            </div>
            <div id="navBtns">
                <button id="signupBtn"><a href="/users/signup">Sign up</a></button>
            </div>
        </nav>
    </header>
    <main id="userAuthMain">
        <section id="loginSection">
            <h2>Login</h2>
            <span id="authMsg">{{message}}</span>
            <form id="login" method="POST" action="/users/login">
                <input
                    type="text" name="user_name"
                    placeholder="Enter Username" required="required"/>
                <input
                    type="password" name="user_password"
                    required="required"
                    placeholder="Enter Password"/>
                <button>login</button>
        </section>
    </main>
</body>
</html>